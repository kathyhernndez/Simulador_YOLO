services:
  tesis-trafico-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tesis_trafico_streamlit
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    restart: unless-stopped
